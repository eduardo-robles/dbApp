drop table if exists Employees;

CREATE TABLE Employees (
fName text NOT NULL,
lName text NOT NULL,
title text NOT NUll,
cell integer NOT NULL,
primaryContactNum integer NOT NULL,
secondaryContactNum integer NOT NULL,
email text NOT NULL
);
